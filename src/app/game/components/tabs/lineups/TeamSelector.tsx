import React from 'react'

type Props = {
    game: any,
    selectedIndex: any,
    setSelectedIndex: any,
}

const TeamSelector = ({ game, selectedIndex, setSelectedIndex }: Props) => {
    return (
        <div>TeamSelector</div>
    )
}

export default TeamSelector